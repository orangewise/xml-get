#!/usr/bin/env node

const xmlGet = require('../.')
const args = process.argv.slice(2)

// process arguments
const path = args[0]
const xml = args[1]
if (!path || !xml) {
  console.error('usage: npx xml-get <json-path> <path-to-xml-file>')
  process.exit(1)
}

console.log(xmlGet.value(path, xml))
